# 决策记录

记录重要取舍、方向改变、技术方案选择和反复讨论后的结论。

## 记录格式

```markdown
## YYYY-MM-DD 决策标题

背景：
- ...

决策：
- ...

原因：
- ...

影响：
- ...

相关文件：
- ...
```

## 当前决策

## 2026-07-14 方形动作反馈与骨架预览恢复

背景：
- 极细网页提示条无法看清方向箭头。
- 摄像头识别已在 Offscreen Document 生效，但控制窗口没有显示手部骨架，用户无法直观看到关键点追踪。

决策：
- 网页右侧提示固定为 88×88 的黑色正方形，不再因悬停或计时缩成竖条；识别动作时在正方形中闪绿色大箭头。
- Offscreen 识别每秒最多 15 次把手部状态和 21 点关键点传给控制窗口，并复用 `CameraOverlay` 绘制骨架。

原因：
- 方形足够小，不遮挡阅读，同时能清楚展示单个方向。
- 关键点预览把“摄像头有画面”“模型看见手”和“挥动被判定”为三个可见状态，排错不会再靠猜。

影响：
- 关键点仅在本机 Chrome 扩展上下文内传递，不上传。
- 预览和后台识别使用同一摄像头，骨架画面会有轻微帧差，但不影响手势动作判断。

## 2026-07-14 预览与收起规则修正

背景：
- 后台识别迁移后，控制窗口只显示 `BACKGROUND ACTIVE`，用户无法直观看到摄像头是否真的在工作。
- 用户要求鼠标停留在控制窗口时不得自动收起。

决策：
- 控制窗口保留一条只用于预览的本地摄像头流，后台页面仍独立负责识别。
- Chrome 已有摄像头授权时不强制重复弹窗；界面显示实时预览即代表权限和预览流已成功启动。
- 展开窗口只在鼠标移出后收起，不再使用停留 5 秒的自动收起规则。

原因：
- 预览让摄像头状态可验证，后台架构仍确保点击网页后识别不停。

影响：
- 摄像头在 Chrome 内由预览和后台识别共用；后台识别仍是唯一的动作来源。

## 2026-07-14 后台识别与网页内反馈

背景：
- 用户点击网页后，独立控制窗口被 Chrome 压到后台或关闭，导致前台窗口里的摄像头识别无法可靠工作。
- 仅把识别挪到后台会失去可见反馈，用户无法判断手势是否被检测。

决策：
- 控制窗口仅承担首次摄像头授权；`offscreen` 后台页面承担持续摄像头、Worker 和挥动识别。
- 在关联网页内注入固定右侧提示条，平时极细纯黑；悬停展开，动作触发时闪绿色方向箭头。
- `HandTracker` 使用摄像头视频帧回调，不依赖可能被后台限速的 `requestAnimationFrame`。

原因：
- 后台识别不会因用户点击网页而失焦或被关闭。
- 网页内提示条属于受控网页本身，点击页面后仍可见，不需要 Chrome 提供置顶浮窗能力。

影响：
- 新增 `offscreen` 权限和后台文档；运行时仍不上传摄像头画面、关键点或模型结果。
- 需要用户在真实 Chrome 中重载扩展并确认首次授权后的后台识别行为。

## 2026-07-14 紧凑标签悬停与方向反馈

背景：
- 用户不希望为了查看手势状态而点击小标签，并希望直接知道检测到了哪个方向。

决策：
- 小标签悬停时自动展开；离开约半秒或展开后停留 5 秒自动收回。
- 小标签平时保持纯黑；每次动作触发时，只短暂显示对应的单个绿色大箭头。

原因：
- 手势控制应该是低打扰的旁路反馈，不应要求额外鼠标点击。

影响：
- 紧凑模式平时隐身，动作发生时才成为识别方向的可视化验证入口。

## 2026-07-14 控制窗口自动收起

背景：
- 独立控制窗口已解决 macOS 摄像头授权，但完整预览窗口遮挡网页和视频。

决策：
- 摄像头启动 5 秒后，将控制窗口缩为屏幕右侧 92×142 的窄标签。
- 窄标签仍保持摄像头和手势检测，点击后恢复完整控制窗口；完整窗口也提供手动收起。

原因：
- 保留浏览器扩展可获得摄像头授权的架构，同时让浏览内容回到主角位置。
- Chrome 扩展不能稳定创建 macOS 级别的贴边自动探出浮层，因此先使用可控、可回退的紧凑窗口。

影响：
- 用户无需每次手动最小化控制窗口。
- 需要在目标 MacBook 上确认紧凑窗口在常用显示器布局中的实际位置。

## 2026-07-12 独立 Web Demo

背景：
- 黑洞源码来自《火星先遣队》，当前项目另有手部识别参考截图。

决策：
- 在当前根目录建立独立应用，只复制黑洞思路和源码，不修改旧游戏项目。
- 第一版使用一只手；左右手功能相同，仅以红蓝区分。

原因：
- 降低旧游戏的复杂度，先验证摄像头控制手感和跨设备可行性。

影响：
- 当前项目拥有自己的依赖、测试、构建和部署入口。

## 2026-07-12 手势与性能方案

背景：
- 用户需要位置转向、张手继续、握拳暂停，并增加手掌左右旋转控制。
- MediaPipe 同步运行在主线程时，真实浏览器实测最低约 19 FPS。
- 3DXRguy 的 VIVERSE 源码调研显示其使用 `HandLandmarker`、GPU 优先、逐点平滑和短暂丢失续帧。

决策：
- 使用 `HandLandmarker` float16 模型和 21 个关键点，不使用 `GestureRecognizer` 内置分类。
- 手腕到中指根部的屏幕平面角度映射为虫洞旋转。
- 手掌倾斜控制的是旋转速度而非固定角度：保持倾斜会持续旋转，回正后停止。
- 张手、握拳和捏合由关节距离计算，收拇指不影响张手判断。
- 前进速度由手掌在画面中的相对尺寸控制：首次识别校准为 1×，靠近摄像头更快、远离更慢，并限制在 0.45×～2.4×。
- MediaPipe WASM 推理放入 ES Module Web Worker，自适应 24–30 FPS，GPU 优先、CPU 备用。
- 关键点逐点平滑；检测短暂失败时保持最后可信姿态 220ms。

原因：
- 关节距离判断不依赖拇指和内置标签；Worker 避免阻塞 Three.js 渲染。

影响：
- 摄像头开启后的黑洞渲染恢复约 60 FPS；模型和 WASM 从本项目同源加载。
- 极端 90° 侧面仍受单目 RGB 摄像头物理限制，但短时遮挡和拇指收拢的体验会明显改善。

## 2026-07-13 Chrome 插件优先

背景：
- 用户希望把摄像头手势能力用于网页或 MacBook 控制，具体载体尚未确定。
- 现有 HandLandmarker、Worker、平滑和骨架预览均为 Web 技术，可以直接复用。

决策：
- 第一版做 Chrome Manifest V3 插件，不做 macOS 原生应用。
- 使用 Side Panel 常驻摄像头和模型；Service Worker 与动态注入脚本控制当前标签页。
- 上下挥动滚动 75% 视口；左右挥动发送 ArrowLeft / ArrowRight，不控制浏览器历史。
- 使用 `activeTab`、`scripting`、`sidePanel` 最小权限，不申请 `<all_urls>`。
- 通用动作后预留 `PageActionAdapter`，按网站逐步补兼容。

原因：
- 浏览器路线可最大化复用现有代码，最快验证用户是否真的愿意使用手势浏览。
- Side Panel 比点击后关闭的 Popup 更适合持续摄像头识别。
- 合成键盘事件不是可信硬件输入，提前建立适配层比宣称全站通用更诚实。

影响：
- 新增 `dist-extension/` 构建，黑洞 Demo 继续保留在 `dist/`。
- 切换新标签页后需要重新点击插件图标授权。
- 如果浏览器版验证成功，再复用 `SwipeDetector` 开发 macOS 全局输入层。

## 2026-07-14 用控制窗口替代 Side Panel

背景：
- 用户在 macOS Chrome 的 Side Panel 中启动摄像头，Chrome 网页权限和系统权限均正常，但 `getUserMedia()` 仍返回 `NotAllowedError`。
- Chromium 扩展支持渠道已确认：Side Panel 没有摄像头/麦克风授权弹窗所需的交互界面。

决策：
- 不再由 Chrome Side Panel 承载摄像头。
- 用户从目标网页点击插件图标后，Service Worker 注入网页控制脚本，并打开独立的控制窗口；该窗口在用户点击“启动摄像头”时请求权限。
- 控制窗口 URL 带原始 tabId，后续挥动始终控制触发它的网页，而不是控制窗口自身。

原因：
- 这是 Chrome Side Panel 在 macOS 上的产品限制，继续调权限没有意义。
- 控制窗口保留常驻体验，同时拥有授权摄像头的正常交互环境。

影响：
- 移除 `sidePanel` 权限；用户不再从 Chrome 侧栏菜单打开插件。
- 版本升级到 `0.3.0`，需要用户在 `chrome://extensions/` 重新加载解压扩展。

# 决策记录

记录重要取舍、方向改变、技术方案选择和反复讨论后的结论。

## 记录格式

```markdown
## YYYY-MM-DD 决策标题

背景：
- ...

决策：
- ...

原因：
- ...

影响：
- ...

相关文件：
- ...
```

## 当前决策

## 2026-07-15 v0.7.2 食指尖轨迹与严格握拳食指姿态

背景：
- `0.7.1` 虽改用 `Pointing_Up`，但移动轨迹仍错误取自手掌中心，用户只移动食指时很难达到跨中线距离；分类置信度单帧下降还会立即清空起点。

决策：
- 翻页起点、距离、水平主导和跨中线全部改用镜像后的 `indexTip` 坐标，手掌中心不再参与食指翻页。
- 食指姿态要求拇指、中指、无名指、小指收拢，仅食指伸直；严格 OK 捏合仍优先。
- 从首次稳定候选帧保存指尖起点；正式激活后允许 `Pointing_Up` 分类波动 180ms，只要关键点仍满足严格食指姿态就继续跟踪。

原因：
- 动作意图来自指尖轨迹，整只手不应被迫横移；短暂分类容错能覆盖挥动过程中常见的姿态置信度波动，同时严格姿态防止普通指向误触。

影响：
- 升级为 `0.7.2`（build 12）；`macos-v0.7.1` 保持为本轮修改前回滚点。

相关文件：
- `macos-app/Sources/GestureControlCore/GestureEngine.swift`
- `macos-app/Sources/GestureControlCore/HandPose.swift`
- `macos-app/Tests/GestureControlCoreTests/GestureEngineTests.swift`

## 2026-07-15 v0.7.1 食指替换 V 手势翻页

背景：
- V 手势需要同时稳定伸出两根手指，高频翻页时不如单食指省力；用户希望把 V 留给以后更独特的操作。

决策：
- 使用 MediaPipe 原生 `Pointing_Up` 接管跨中线翻页：左向右下翻，右向左上翻。
- 保留 0.70 置信度、220ms 稳定、7% 起点保护、14% 最小位移、1.4 倍水平主导、真实跨中线和退出 150ms 后重激活。
- 严格 OK 捏合优先于食指分类；V 手势只显示“暂未设置操作”，不输出系统事件。

原因：
- 单食指更容易保持且侧面轮廓更清楚；跨中线和稳定门槛可以抵消日常指向动作更常见带来的误触风险。

影响：
- 升级为 `0.7.1`（build 11）；`macos-v0.7.0` 保持为本轮修改前回滚点。

相关文件：
- `macos-app/Sources/GestureControlCore/GestureEngine.swift`
- `macos-app/Sources/GestureControlCore/HandPose.swift`
- `macos-app/Sources/GestureControlApp/AppModel.swift`
- `macos-app/Tests/GestureControlCoreTests/GestureEngineTests.swift`

## 2026-07-15 v0.7.0 捏合导航方向锁与 V 跨中线翻页

背景：
- 捏合横向移动若直接映射视频进度条，依赖网站结构且容易与垂直滚动冲突；张开手掌又是最常见姿态，不适合继续承担高误触风险动作。

决策：
- 严格 OK 捏合先进入方向未确定状态，再按水平/垂直主导比例锁定到滚动或导航，松开前不能切换。
- 左侧捏合向右跨中线返回，右侧捏合向左跨中线前进；必须满足起点保护、14% 最小位移和真实跨中线，每次捏合只触发一次。
- V 手势接管上下翻页，左向右跨中线下翻、右向左跨中线上翻；张开手掌只显示识别状态。
- 返回/前进只允许 Chrome、Safari、Edge 和夸克，并在丢手、切换 App、暂停、切换控制手或重启识别器时清空状态。

原因：
- 一次手势只承担一个已锁定意图，能显著降低斜向漂移误判；跨中线动作也比按固定像素位移更容易理解和校准。

影响：
- 升级为 `0.7.0`（build 10）；`macos-v0.6.0` 保持为本轮修改前回滚点。

相关文件：
- `macos-app/Sources/GestureControlCore/GestureEngine.swift`
- `macos-app/Sources/GestureControlApp/AppModel.swift`
- `macos-app/Sources/GestureControlApp/SystemNavigationEmitter.swift`
- `macos-app/Tests/GestureControlCoreTests/GestureEngineTests.swift`

## 2026-07-15 v0.6.0 指定控制手并严格过滤

背景：
- 两只手同时进入画面时，单手识别结果可能在左右手之间切换，导致非预期动作。

决策：
- 主控制窗口新增右手/左手互斥选择，默认右手并持久保存。
- MediaPipe 与 Apple Vision 都保持单手推理；关键点进入 `GestureEngine` 前按选择严格过滤，未选择手和未知左右手数据都不能执行动作。
- 切换控制手时立即取消捏合、挥手、返回和点赞状态；校准窗口也不绘制未选择手骨架。

原因：
- 在唯一动作入口过滤可覆盖所有现有和未来手势，同时避免把 `numHands` 提到 2 后增加计算量。

影响：
- 升级为 `0.6.0`（build 9）；`macos-v0.5.1` 保持为修改前回滚点。

相关文件：
- `macos-app/Sources/GestureControlApp/AppModel.swift`
- `macos-app/Sources/GestureControlApp/MenuBarView.swift`
- `macos-app/Sources/GestureControlCore/HandPose.swift`
- `src/native-recognizer.ts`

## 2026-07-15 v0.5.1 固定反馈条、纵向迷你条与双击暂停

背景：
- `0.5.0` 用 SwiftUI 全局拖动坐标驱动正在移动的 `NSPanel`，坐标系不断重算导致窗口明显颤动。
- 用户不再需要展开窗口自由摆放，需要更简单的收起、暂停与恢复入口。

决策：
- 展开状态条固定底部居中，保留右侧三道杠作为单击收起按钮。
- 收起后只在右边缘显示左圆右方迷你条，它使用 `NSEvent.mouseLocation` 的屏幕绝对坐标锁定水平位置，只允许上下移动。
- 双击展开状态内容切换暂停/恢复；暂停会停止摄像头、识别器和系统事件，但保留红色反馈窗口。

原因：
- 固定的交互路径比自由拖动更稳，绝对鼠标坐标不会随窗口自身移动而反向变化。

影响：
- 升级为 `0.5.1`（build 8）；旧左右吸附与水平位置设置被清理，只保留收起状态和迷你条纵向位置。
- 暂停状态不跨 App 重启保存，不改变原有手势算法。

相关文件：
- `macos-app/Sources/GestureControlApp/ScreenGestureOverlay.swift`
- `macos-app/Sources/GestureControlApp/AppModel.swift`

## 2026-07-15 v0.5.0 独立可拖拽状态反馈层

背景：
- 实时反馈和全屏手部骨架原本在同一个鼠标穿透窗口，状态条因此无法拖动，有时会遮挡正在阅读的内容。

决策：
- 骨架和状态反馈拆成两个独立的非激活 `NSPanel`：骨架层仍完全穿透，状态层单独接收鼠标拖动。
- 状态条靠近左右边缘时吸附并收成只有状态圆点的窄标签；点击或向屏幕内拖动恢复。
- 使用 `UserDefaults` 保存位置、吸附边和收起状态；外接屏幕变化后自动将窗口校正回可见区。

原因：
- 一个窗口无法同时做到“全屏点击穿透”和“局部可拖动”；拆层是最稳定、对识别性能零影响的方案。

影响：
- 升级为 `0.5.0`（build 7）；不改变识别、捏合、翻页、返回和点赞算法。
- 状态条的鼠标交互不会让 GestureControl 变成前台 App，浏览器仍是手势事件目标。

相关文件：
- `macos-app/Sources/GestureControlApp/ScreenGestureOverlay.swift`

## 2026-07-15 v0.4.1 固定本地签名身份

背景：
- 系统设置中的 GestureControl 开关显示开启，但 App 的 `AXIsProcessTrusted()` 返回 false。
- 原 ad-hoc 签名没有内部 designated requirement，系统只能按每次构建变化的 CDHash 识别二进制。

决策：
- 构建脚本为 ad-hoc App 写入 `designated => identifier "com.huyee.gesture-control.prototype"`。
- 只重置一次 GestureControl 的旧 Accessibility TCC 记录，再由用户打开最终安装版的新开关。

原因：
- 固定 requirement 能让后续本地重建继续匹配同一权限身份；其他应用的权限不受影响。

影响：
- 升级为 `0.4.1`（build 6）；首次修复仍需要用户亲自确认一次系统安全开关。
- 这只解决本机开发签名稳定性，外部分发仍需 Developer ID 和 notarization。

## 2026-07-15 v0.4.0 Gesture Recognizer、严格 OK 捏合与浏览器返回

背景：
- 用户需要高识别率的返回和点赞姿态，并发现普通握拳容易被两指距离规则误判为捏合。
- 当前 MediaPipe 已配置最多双手，但 App 只消费一只手，产生无效计算。

决策：
- 主模型改用 MediaPipe Gesture Recognizer，`numHands` 固定为 1；保留 21 点用于原有翻页、骨架和连续滚动。
- 捏合必须同时满足拇指食指接触及中指、无名指、小指张开，即严格 OK 手势。
- `Victory` 稳定 220ms 后向物理左侧移动 14% 触发浏览器返回；`Thumb_Up` 稳定 300ms 只显示测试状态。
- 返回只对 Chrome、Safari、Edge、夸克发送 `Command + [`；未来双手采用先稳定做出有效手势者锁定控制权。
- `macos-v0.3.0` 保持不变并标记为 ⭐ 重要稳定版；新功能进入 `0.4.0`。

原因：
- 官方手势分类比继续堆几何阈值更适合区分 V、竖拇指和握拳；严格 OK 条件能直接消除握拳误触。
- 单手上限符合当前产品范围，减少第二只手检测的无效负担。

影响：
- Apple Vision 备用模式继续支持旧手势，但不提供 V 返回和点赞分类。
- 系统首次新增受限键盘事件；仍不移动鼠标、不点击网页、不执行真实点赞。

相关文件：
- `src/native-recognizer.ts`
- `macos-app/Sources/GestureControlCore/GestureEngine.swift`
- `macos-app/Sources/GestureControlApp/AppModel.swift`

## 2026-07-15 MediaPipe 主引擎、HID 滚动与正常 Dock App

背景：
- Apple Vision 官方明确提示：手掌接近画面边缘、遮挡或与摄像头视线平行时可能表现不佳；用户实测侧手超过约 90°容易丢失。
- 浏览器版 MediaPipe 已验证 21 点、左右手和侧手跟踪更稳定，用户要求两套技术择优。
- 旧系统滚动通过 `postToPid` 发送，Chromium 可能忽略这类 PID 定向的设备滚轮事件。
- macOS 记住菜单栏项目为隐藏后，会直接结束只有 `MenuBarExtra` 的 App，导致退出后找不到入口。

决策：
- macOS App 以 MediaPipe Hand Landmarker 为主引擎：GPU 优先、CPU 备用、最多双手检测、选择一只手连续跟踪；Apple Vision 仅在 MediaPipe 无法启动时兜底。
- MediaPipe WASM 与模型打包在 App 内，由进程内仅监听 `127.0.0.1` 的静态服务提供给 WKWebView；不联网、不上传画面。
- 系统滚动通过 `.cghidEventTap` 注入，并把事件位置放到目标前台窗口中心。
- App 同时提供正常 Dock 控制窗口与菜单栏入口；校准视频、骨架和全屏 HUD 使用一致的自拍镜像坐标。

原因：
- 这条路线直接复用已经验证过的识别精度，并绕开 Apple Vision 对侧手姿态的已知限制。
- HID event tap 更接近真实鼠标/触控板事件，能由 macOS 正常路由到 Chromium。
- 正常 App 生命周期能彻底避免菜单栏隐藏导致的启动即退出，也让用户可以从“应用程序”重新进入。

影响：
- macOS 原型升级为 `0.3.0`；安装包约增加 40 MB 的本地模型与 WASM 资源。
- 最终 ad-hoc 二进制替换后仍需重新授权一次辅助功能；真实 Chrome 滚动方向和误触率待用户验收。

相关文件：
- `src/native-recognizer.ts`
- `macos-app/Sources/GestureControlApp/MediaPipeHandPoseService.swift`
- `macos-app/Sources/GestureControlApp/LocalMediaServer.swift`
- `macos-app/Sources/GestureControlApp/SystemScrollEmitter.swift`

## 2026-07-15 全屏 21 点手势 HUD 与误识别过滤

背景：
- 摄像头预览和简化骨架水平错位；只取 10 点无法表达完整手指姿态。
- 手离开后 Vision 会在脸部附近留下稀疏假点云；旧置信度取所有点最低值，又会让真实手掌可见但动作引擎拒绝执行。

决策：
- 使用 Apple Vision 完整 21 点，骨架与自拍预览使用同一水平坐标。
- 只有点数、完整手指链、点云跨度满足条件的结果才作为手掌；姿态置信度改用关节点中位数。
- 默认显示点击穿透的全屏透明 HUD；摄像头窗口降级为可选校准工具。
- 动态说明张手、握拳、单指、捏合与翻页结果，并提供独立的系统下翻测试按钮。

原因：
- 系统级控制的核心反馈应覆盖当前工作屏幕，而不是绑在摄像头小窗里。
- 识别反馈、姿态分类和系统事件必须能分段诊断，否则“没有反应”无法判断是哪一层出错。

影响：
- macOS 原型升级为 `0.2.0`；全屏 HUD 不拦截鼠标，也不读取屏幕。
- 真实手势阈值和滚动方向仍需用户实机验收。

相关文件：
- `macos-app/Sources/GestureControlApp/ScreenGestureOverlay.swift`
- `macos-app/Sources/GestureControlApp/HandPoseService.swift`
- `macos-app/Sources/GestureControlCore/HandPose.swift`

## 2026-07-15 辅助功能权限与悬浮识别反馈

背景：
- ad-hoc 二进制更新后，macOS 辅助功能列表仍显示旧授权为开启，但新二进制读取不到权限；启动按钮又自动打开系统设置，形成循环。
- 菜单栏内的小预览无法让用户持续确认手掌是否被识别。

决策：
- 安装路径固定为 `/Applications/GestureControl.app`；替换二进制后只重置一次旧 TCC 记录，再对最终安装版本授权。
- 启动按钮不再自动打开设置；只有用户点击权限行的“设置”按钮才请求辅助功能。App 每秒刷新状态，授权生效后自动继续启动。
- 控制开启后默认弹出悬浮测试窗，显示镜像视频、手部骨架、捏合连线和实时状态。

原因：
- 权限状态必须由 App 实际读取结果决定，不能相信系统设置里同名旧条目的表面开关。
- 系统级输入没有网页叠层，测试阶段必须提供持续、可见的识别反馈。

影响：
- macOS 原型升级为 `0.1.1`；后续重新构建 ad-hoc App 时仍可能需要重置 TCC，正式签名后再消除该开发期限制。
- 悬浮窗只用于状态反馈，不接收手势输入，不改变前台 App 目标。

相关文件：
- `macos-app/Sources/GestureControlApp/AppModel.swift`
- `macos-app/Sources/GestureControlApp/DebugWindow.swift`
- `macos-app/scripts/install-app.sh`

## 2026-07-15 系统级 macOS 原型替代浏览器多页面重构

背景：
- 浏览器插件多页面不稳定来自 `activeTab`、MV3 生命周期、页面注入和标签页绑定，不是手势识别算法本身。
- 用户的真实目标是 MacBook 上跨应用阅读控制，而不是只控制 Chrome。

决策：
- 浏览器插件冻结在本地 `v1.0.1`，第 04 阶段改做原生 macOS 菜单栏 App。
- 第一版使用 AVFoundation + Apple Vision + Core Graphics，只实现左挥上翻、右挥下翻和捏住上下拖动滚动。
- 不移动系统光标，不做鼠标点击或 Air Mouse；系统事件只发给当前前台 App，切换 App 立即取消手势。

原因：
- 系统级事件消除标签页、网页权限和 Content Script 约束，能直接覆盖 Chrome、Safari、Preview、Notion 等场景。
- Air Mouse 会引入光标映射、抖动、误点和疲劳，不能与最小翻页原型混在一起验证。

影响：
- 新增独立 `macos-app/` 版本线，原型版本从 `0.1.0` 开始。
- 需要摄像头和辅助功能权限，不需要屏幕录制权限。
- 浏览器插件代码和历史版本完整保留，但暂不继续多页面优化。

相关文件：
- `macos-app/`
- `docs/production-blueprint.md`
- `docs/dev-log/archive/03-browser-extension-v1-pivot.md`

## 2026-07-14 常驻高级设置的原生窗口高度

背景：
- 用户安装 1.0.0 后确认版本正确，但控制窗口中完全看不到“高级设置”。

决策：
- 默认控制窗口从 300×350 增至 300×390；高级设置展开态仍为 300×640。

原因：
- 350px 是加入常驻设置前的旧尺寸，页面内容被原生窗口裁在底部，HTML 元素存在但无法看到。

影响：
- 预览窗口略高，但无需滚动即可看到高级设置入口。

相关文件：
- `extension/service-worker.ts`

## 2026-07-14 1.0 打包与控制窗口停留规则

背景：
- 用户确认核心手势控制达到 1.0 可交付状态，但发现高级设置在摄像头未运行时消失，且点击网页会因窗口失焦过早收起。
- 产品需要同时面向 Chrome 与 Microsoft Edge（Chromium），并形成可上传商店的可复现包。

决策：
- 高级设置入口始终放在摄像头开关下方；展开后显示两条灵敏度滑杆与网格、捏合圆点两个开发者开关。
- 只以窗口 `pointerleave` 作为收起触发条件；删除 `blur` 收起路径。鼠标在主窗口范围内时，窗口不会消失。
- 固定 `npm run package:extension` 为 1.0 发布包命令，生成 manifest 位于 ZIP 根目录的 MV3 包；Chrome 与 Edge 共用同一构建产物。

原因：
- 高级设置是工具入口，不是摄像头运行状态；不该无故消失。
- 失焦并不等于用户离开控制窗口，尤其点击网页正是使用手势控制的常规动作。
- 本项目使用的 `offscreen`、`scripting`、`storage` 等 MV3 API 均在 Microsoft Edge 的官方支持列表中，适合用同一代码包作为两个商店的基础。

影响：
- 展开高级设置后即使摄像头关闭，也能预先调整并持久化参数；设置会在下次启动立即生效。
- ZIP 本身用于商店提交；本机开发安装仍必须选择解压后的 `dist-extension/`。

相关文件：
- `extension/sidepanel.html`
- `extension/sidepanel.ts`
- `scripts/package-extension.mjs`
- `README.md`

## 2026-07-14 默认隐身的网页调试叠层

背景：
- 用户确认页面控制更稳定，但不再需要常驻捏合圆点；它和手部网格会让阅读网页显得像调试器。
- 用户仍需要在必要时看到手部骨架与捏合位置，用来精确调节阈值。

决策：
- 网页默认不显示捏合圆点或手部网格。
- 摄像头启动后，在启停按钮下方显示“高级模式”；其中保留左右挥手、手指捏合灵敏度滑杆，并新增“显示手部网格”“显示捏合控制圆点”开关，默认均关闭。
- 四项设置保存在本机扩展存储，并在启动、网页脚本重注入和控制窗口关闭恢复时同步。

原因：
- 正常使用时界面应隐身；可视化只能是排障工具，不该成为阅读内容的一部分。

影响：
- 高级模式展开时控制窗高度为 640px；关闭摄像头时高级模式随之隐藏。

相关文件：
- `extension/content-script.ts`
- `extension/service-worker.ts`
- `extension/sidepanel.html`
- `src/gesture-settings.ts`

## 2026-07-14 网页真实反色、关闭恢复与可调灵敏度

背景：
- 用户实机确认捏合点在浅色和蓝色页面上仍显示为白色，说明 Shadow DOM 内的 `mix-blend-mode` 没有得到网页像素作为混合背景。
- 关闭预览窗口后，用户遇到网页不再响应手势；同时需要按个人习惯微调横挥与捏合门槛。

决策：
- 捏合点移出 Shadow DOM，作为网页 Light DOM 顶层节点用 `backdrop-filter: invert(1)` 直接反转它覆盖的网页画面。
- 原生控制窗口关闭时，Service Worker 读取后台状态、再次确认/注入网页接收器，再恢复入口黑点与识别状态；不把控制窗口生命周期当作摄像头生命周期。
- 新增本机持久化的 0–100 高级设置：挥手值越高，需要的横向位移越短；捏合值越高，允许的指尖间隙略大。默认 50 仍为历史阈值。

原因：
- 真正的 backdrop filter 才能反映图片、渐变和分区背景，而不是只猜某个 CSS 颜色。
- “预览关了，控制还在”必须由恢复链路显式保证，不能只依赖 Offscreen Document 理论上还活着。

影响：
- 新增 `storage` 权限，仅保存两项本机数值，不保存摄像头画面或手势轨迹。
- 展开高级设置时原生控制窗口高度增至 540px；收起后恢复紧凑尺寸。

相关文件：
- `extension/content-script.ts`
- `extension/service-worker.ts`
- `extension/offscreen.ts`
- `src/gesture-settings.ts`

## 2026-07-14 反色捏合点与控制窗口收口

背景：
- 用户希望捏合点在浅色、深色网页上都清楚可见，且不要绿色、投影或比入口更大的视觉存在。
- 展开控制窗口时不应同时露出右侧入口黑点；启停控件也不应像播放器。

决策：
- 捏合点使用 28px 的白色 DOM 圆点与 `mix-blend-mode: difference`，让浏览器按其下方网页像素自动反色；不加投影。
- 入口也保持 28px，位置下移；控制窗口右缘贴齐 Chrome 内容区，并在展开态显式隐藏入口。
- 原生控制窗口收紧为 300×350，保留 16:9 视频，启停改为满宽圆角文字按钮“开启摄像头 / 关闭摄像头”。

原因：
- 反色是唯一不需要猜测网页背景色、又能保持极简的方案。
- 入口与控制点同尺度，窗口从入口展开的关系更干净，不再像两个孤立的黑块。

影响：
- `mix-blend-mode` 只影响网页内视觉层，不会改变网页内容或系统鼠标；捏合与滚动判定逻辑不变。

相关文件：
- `extension/content-script.ts`
- `extension/service-worker.ts`
- `extension/sidepanel.css`
- `extension/sidepanel.ts`

## 2026-07-14 严格捏合与贴边紧凑控制窗口

背景：
- 用户反馈此前捏合阈值过宽，食指和拇指尚未贴合时也会出现网页控制点；15 FPS 网页状态更新让点的移动可见跳帧。
- 用户希望把控制窗口压缩、移动到网页右侧小黑点附近，并有从小点展开、点击网页后收回的视觉关系。

决策：
- 捏合分类和捏合滚动均以归一化距离 `0.18` 为接触阈值；一旦超过阈值立即取消网页点和连续滚动。
- Offscreen 状态同步提升到 30 FPS；Content Script 使用动画帧插值显示 36px 不透明绿色点与极轻投影。
- 原生 Chrome 弹窗不能嵌入网页，因此控制窗口采用 300×400 的贴边原生窗口；Service Worker 依据 Chrome 窗口坐标把它放在右侧中部。网页小黑点在原生窗口开关时缩放淡入淡出，提供等效的展开/收起动效。

原因：
- “严格接触”是精细控制的显式离合器，能避免普通手势误进入滚动模式。
- 状态频率与页面渲染频率分层后，视觉更平滑而不需要提高模型推理或上传任何数据。

影响：
- 实际原生窗口仍由 Chrome 管理，无法与网页 DOM 完全合并或逐帧跟随；网页圆点承担连续性动画。

相关文件：
- `src/hand-gesture-math.ts`
- `extension/content-script.ts`
- `extension/service-worker.ts`

## 2026-07-14 网页极简反馈与横挥上下滚动

背景：
- 用户不需要持续的手部网格或黑色动作面板，只需要明确知道“已捏合”和“本次会向哪里滚动”。
- 原来的左右挥动翻页难以在普通网页稳定验证，且和捏合滚动不是同一套阅读模型。

决策：
- 网页仅在捏合时显示无特效的半透明绿色中点；松开、丢手或识别过期立即清空。
- 左挥映射上滚并显示中央空心 `↑`，右挥映射下滚并显示中央空心 `↓`；不再发送合成方向键。
- 网页入口缩为右侧 28px 黑色圆点；控制窗口删除动作屏，只保留摄像头预览与启停。

原因：
- 反馈与用户的实际意图一一对应，同时不占据阅读区域。
- 右挥下翻和捏合上拖下翻形成两条可直接对比的“向下浏览”路径。

影响：
- 左右翻页能力从本轮产品范围移除；未来若需要站点级翻页，应以单独适配器重新引入。

相关文件：
- `extension/content-script.ts`
- `src/swipe-detector.ts`
- `gesture-test.html`

## 2026-07-14 网页接收器以确认式注入恢复

背景：
- 用户实机截图显示 `Could not establish connection. Receiving end does not exist.`；摄像头骨架正常，说明问题发生在后台到网页的最后一跳。
- 旧实现只要 `executeScript()` Promise 返回就认为网页控制脚本可用，且构建产物带有运行时模块依赖；这不够可靠。

决策：
- 网页 Content Script 改为无运行时 import 的独立文件。
- 注入后立即 ping 确认接收器已建立；动作或网页反馈若发现接收器消失，自动补注入后只重试一次。

原因：
- 动作链路必须以“网页确实收到消息”为成功条件，而不是以“尝试注入没有报错”为成功条件。

影响：
- 页面跳转、扩展重载后可自行恢复；正常捏合拖动不再每帧 ping，避免消息检查影响滚动手感。

相关文件：
- `extension/content-script.ts`
- `extension/service-worker.ts`

## 2026-07-14 网页消息按接收方严格过滤

背景：
- 加入网页手部网格后，Offscreen Document 会持续发布识别状态。
- Chrome 会将扩展 runtime 消息送达 Content Script；旧代码没有先确认消息属于网页协议，可能把后台状态或捏合请求落进通用网页动作分支，导致页面不滚动。

决策：
- Content Script 只接受 `ContentScriptRequest` 白名单中的六种消息；其他扩展内部消息直接忽略。

原因：
- 后台识别、Service Worker 路由和网页执行是三个独立接收方，消息类型必须明确隔离，不能依赖发送方“刚好不被收到”。

影响：
- 手部网格、黄色黄点和捏合/左右动作可同时运行；背景识别消息不再有机会干扰网页控制。

相关文件：
- `extension/message-types.ts`
- `extension/content-script.ts`

## 2026-07-14 反向捏合拖动与网页手部指针

背景：
- 用户认为手向上移动的意图是查看下方内容；此前实现按同向滚动，导致感知与结果相反。
- 视频控制窗口关闭后，用户无法确认后台模型看到的手在网页空间中的位置。

决策：
- 捏合拖动采用触控板/触屏的直接操作模型：手向上拖动时文档内容向上移动，页面滚动向下，从而显示下方内容。
- 后台识别每秒最多 15 次把手部状态转发到受控网页；网页以镜像坐标绘制 21 点手部网格，捏合时在拇指尖与食指尖中点绘制黄色球。

原因：
- 内容与手的移动方向一致，符合用户“把页面往上推”的直觉。
- 黄色球是明确的空间锚点，能区分“模型没看到手”和“捏合还没接管滚动”。

影响：
- 网页叠层仅在本机渲染，不上传画面或关键点；全屏画布不接收指针事件，不阻挡网页点击。
- 黄色球的坐标是摄像头画面到当前网页视口的镜像映射，不是系统级鼠标控制。

## 2026-07-14 网页方形提示成为控制入口

背景：
- 用户认为插件图标更像“重新启动”，而运行中的网页方形提示才是手势控制的自然入口。
- 旧控制窗口下方使用四方向按钮阵列，重复表达了动作状态，视觉过重。

决策：
- 点击网页内 88×88 方形提示，直接打开关联标签页的摄像头控制窗口；插件图标保留作第一次启动和备用入口。
- 控制窗口改成视频预览、单一黑色动作屏和一个启动/停止按钮；动作屏只在识别时闪一个绿色箭头。

原因：
- 入口跟随正在运行的功能，用户不必回到浏览器工具栏寻找插件。
- 一个即时动作屏比四个常驻箭头更轻、更像仪表，而不是控制台。

影响：
- 网页提示改为可点击和键盘可访问；点击只打开预览，不会重新启动后台识别。
- 控制窗口从 390×720 收紧为 340×560。

## 2026-07-14 删除冗余紧凑控制窗口

背景：
- 用户点击网页后，独立控制窗口会缩成一个黑色窄标签；网页本身又有一个黑色方形动作提示，造成两个黑块。
- Offscreen Document 已独立持有摄像头和手势识别，不需要紧凑窗口继续保活。

决策：
- 删除紧凑控制标签及其箭头反馈。
- 鼠标离开控制窗口后直接关闭它，只保留网页内的 88×88 方形提示；用户需要停止摄像头时重新点击插件图标打开控制窗口。

原因：
- 一个明确的网页内状态点已经够了；第二个黑块没有提供额外信息，只会遮挡内容。

影响：
- 关闭控制窗口会停止其预览流，但不会停止后台识别、捏合滚动或左右翻页。

## 2026-07-14 捏合连续滚动替代上下挥动

背景：
- 上下挥动没有稳定的身体参照，用户实机测试中容易把方向或动作本身做错。
- 当前 HandLandmarker 已经输出拇指尖和食指尖的 21 点关键点，工程也已有归一化捏合距离计算。

决策：
- 上下滚动改为拇指和食指捏住后上下拖动：稳定接触约 80ms 才启动，松开即停止；手向上移动时页面向上。
- 保留张开手掌的左右挥动翻页；Extension 禁用上下挥动，以免两个输入模式抢同一条垂直轨迹。
- 控制窗口在两指接触时，将拇指尖与食指尖的中点绘制成黄色球。

原因：
- 捏合是显式的“我要控制”信号，连续拖动更接近鼠标滚轮/触控板而非四个模糊的空中方向。
- 黄色球让用户确认识别的是手势接触点，不是猜模型有没有看见手。

影响：
- 新增 `PinchScrollDetector`，并覆盖启动、拖动、松开/跳变及握拳保护的单元测试。
- 无需新模型、权限或网络请求；仍基于本机 `HandLandmarker` 的拇指尖（4）和食指尖（8）关键点。

## 2026-07-14 方形动作反馈与骨架预览恢复

背景：
- 极细网页提示条无法看清方向箭头。
- 摄像头识别已在 Offscreen Document 生效，但控制窗口没有显示手部骨架，用户无法直观看到关键点追踪。

决策：
- 网页右侧提示固定为 88×88 的黑色正方形，不再因悬停或计时缩成竖条；识别动作时在正方形中闪绿色大箭头。
- Offscreen 识别每秒最多 15 次把手部状态和 21 点关键点传给控制窗口，并复用 `CameraOverlay` 绘制骨架。

原因：
- 方形足够小，不遮挡阅读，同时能清楚展示单个方向。
- 关键点预览把“摄像头有画面”“模型看见手”和“挥动被判定”为三个可见状态，排错不会再靠猜。

影响：
- 关键点仅在本机 Chrome 扩展上下文内传递，不上传。
- 预览和后台识别使用同一摄像头，骨架画面会有轻微帧差，但不影响手势动作判断。

## 2026-07-14 预览与收起规则修正

背景：
- 后台识别迁移后，控制窗口只显示 `BACKGROUND ACTIVE`，用户无法直观看到摄像头是否真的在工作。
- 用户要求鼠标停留在控制窗口时不得自动收起。

决策：
- 控制窗口保留一条只用于预览的本地摄像头流，后台页面仍独立负责识别。
- Chrome 已有摄像头授权时不强制重复弹窗；界面显示实时预览即代表权限和预览流已成功启动。
- 展开窗口只在鼠标移出后收起，不再使用停留 5 秒的自动收起规则。

原因：
- 预览让摄像头状态可验证，后台架构仍确保点击网页后识别不停。

影响：
- 摄像头在 Chrome 内由预览和后台识别共用；后台识别仍是唯一的动作来源。

## 2026-07-14 后台识别与网页内反馈

背景：
- 用户点击网页后，独立控制窗口被 Chrome 压到后台或关闭，导致前台窗口里的摄像头识别无法可靠工作。
- 仅把识别挪到后台会失去可见反馈，用户无法判断手势是否被检测。

决策：
- 控制窗口仅承担首次摄像头授权；`offscreen` 后台页面承担持续摄像头、Worker 和挥动识别。
- 在关联网页内注入固定右侧提示条，平时极细纯黑；悬停展开，动作触发时闪绿色方向箭头。
- `HandTracker` 使用摄像头视频帧回调，不依赖可能被后台限速的 `requestAnimationFrame`。

原因：
- 后台识别不会因用户点击网页而失焦或被关闭。
- 网页内提示条属于受控网页本身，点击页面后仍可见，不需要 Chrome 提供置顶浮窗能力。

影响：
- 新增 `offscreen` 权限和后台文档；运行时仍不上传摄像头画面、关键点或模型结果。
- 需要用户在真实 Chrome 中重载扩展并确认首次授权后的后台识别行为。

## 2026-07-14 紧凑标签悬停与方向反馈

背景：
- 用户不希望为了查看手势状态而点击小标签，并希望直接知道检测到了哪个方向。

决策：
- 小标签悬停时自动展开；离开约半秒或展开后停留 5 秒自动收回。
- 小标签平时保持纯黑；每次动作触发时，只短暂显示对应的单个绿色大箭头。

原因：
- 手势控制应该是低打扰的旁路反馈，不应要求额外鼠标点击。

影响：
- 紧凑模式平时隐身，动作发生时才成为识别方向的可视化验证入口。

## 2026-07-14 控制窗口自动收起

背景：
- 独立控制窗口已解决 macOS 摄像头授权，但完整预览窗口遮挡网页和视频。

决策：
- 摄像头启动 5 秒后，将控制窗口缩为屏幕右侧 92×142 的窄标签。
- 窄标签仍保持摄像头和手势检测，点击后恢复完整控制窗口；完整窗口也提供手动收起。

原因：
- 保留浏览器扩展可获得摄像头授权的架构，同时让浏览内容回到主角位置。
- Chrome 扩展不能稳定创建 macOS 级别的贴边自动探出浮层，因此先使用可控、可回退的紧凑窗口。

影响：
- 用户无需每次手动最小化控制窗口。
- 需要在目标 MacBook 上确认紧凑窗口在常用显示器布局中的实际位置。

## 2026-07-12 独立 Web Demo

背景：
- 黑洞源码来自《火星先遣队》，当前项目另有手部识别参考截图。

决策：
- 在当前根目录建立独立应用，只复制黑洞思路和源码，不修改旧游戏项目。
- 第一版使用一只手；左右手功能相同，仅以红蓝区分。

原因：
- 降低旧游戏的复杂度，先验证摄像头控制手感和跨设备可行性。

影响：
- 当前项目拥有自己的依赖、测试、构建和部署入口。

## 2026-07-12 手势与性能方案

背景：
- 用户需要位置转向、张手继续、握拳暂停，并增加手掌左右旋转控制。
- MediaPipe 同步运行在主线程时，真实浏览器实测最低约 19 FPS。
- 3DXRguy 的 VIVERSE 源码调研显示其使用 `HandLandmarker`、GPU 优先、逐点平滑和短暂丢失续帧。

决策：
- 使用 `HandLandmarker` float16 模型和 21 个关键点，不使用 `GestureRecognizer` 内置分类。
- 手腕到中指根部的屏幕平面角度映射为虫洞旋转。
- 手掌倾斜控制的是旋转速度而非固定角度：保持倾斜会持续旋转，回正后停止。
- 张手、握拳和捏合由关节距离计算，收拇指不影响张手判断。
- 前进速度由手掌在画面中的相对尺寸控制：首次识别校准为 1×，靠近摄像头更快、远离更慢，并限制在 0.45×～2.4×。
- MediaPipe WASM 推理放入 ES Module Web Worker，自适应 24–30 FPS，GPU 优先、CPU 备用。
- 关键点逐点平滑；检测短暂失败时保持最后可信姿态 220ms。

原因：
- 关节距离判断不依赖拇指和内置标签；Worker 避免阻塞 Three.js 渲染。

影响：
- 摄像头开启后的黑洞渲染恢复约 60 FPS；模型和 WASM 从本项目同源加载。
- 极端 90° 侧面仍受单目 RGB 摄像头物理限制，但短时遮挡和拇指收拢的体验会明显改善。

## 2026-07-13 Chrome 插件优先

背景：
- 用户希望把摄像头手势能力用于网页或 MacBook 控制，具体载体尚未确定。
- 现有 HandLandmarker、Worker、平滑和骨架预览均为 Web 技术，可以直接复用。

决策：
- 第一版做 Chrome Manifest V3 插件，不做 macOS 原生应用。
- 使用 Side Panel 常驻摄像头和模型；Service Worker 与动态注入脚本控制当前标签页。
- 上下挥动滚动 75% 视口；左右挥动发送 ArrowLeft / ArrowRight，不控制浏览器历史。
- 使用 `activeTab`、`scripting`、`sidePanel` 最小权限，不申请 `<all_urls>`。
- 通用动作后预留 `PageActionAdapter`，按网站逐步补兼容。

原因：
- 浏览器路线可最大化复用现有代码，最快验证用户是否真的愿意使用手势浏览。
- Side Panel 比点击后关闭的 Popup 更适合持续摄像头识别。
- 合成键盘事件不是可信硬件输入，提前建立适配层比宣称全站通用更诚实。

影响：
- 新增 `dist-extension/` 构建，黑洞 Demo 继续保留在 `dist/`。
- 切换新标签页后需要重新点击插件图标授权。
- 如果浏览器版验证成功，再复用 `SwipeDetector` 开发 macOS 全局输入层。

## 2026-07-14 用控制窗口替代 Side Panel

背景：
- 用户在 macOS Chrome 的 Side Panel 中启动摄像头，Chrome 网页权限和系统权限均正常，但 `getUserMedia()` 仍返回 `NotAllowedError`。
- Chromium 扩展支持渠道已确认：Side Panel 没有摄像头/麦克风授权弹窗所需的交互界面。

决策：
- 不再由 Chrome Side Panel 承载摄像头。
- 用户从目标网页点击插件图标后，Service Worker 注入网页控制脚本，并打开独立的控制窗口；该窗口在用户点击“启动摄像头”时请求权限。
- 控制窗口 URL 带原始 tabId，后续挥动始终控制触发它的网页，而不是控制窗口自身。

原因：
- 这是 Chrome Side Panel 在 macOS 上的产品限制，继续调权限没有意义。
- 控制窗口保留常驻体验，同时拥有授权摄像头的正常交互环境。

影响：
- 移除 `sidePanel` 权限；用户不再从 Chrome 侧栏菜单打开插件。
- 版本升级到 `0.3.0`，需要用户在 `chrome://extensions/` 重新加载解压扩展。
